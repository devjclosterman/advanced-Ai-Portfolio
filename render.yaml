services:
  - type: web
    name: advanced-ai-portfolio
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn backend.fastapi_app:app --host 0.0.0.0 --port 8000"
    plan: free
